Threads : Thread is a light weight smallest part of a program. (a method will be running on one thread)
          We cannot guess how Threads are executed.            (a java project is a process and it has threads)
                                                               (threads are created inside 'stack')
                                                               (as stack stores temp memory, so thread is temporary)
                                                               (one process runs with help of multiple threads running in it)

- Ways to create a thread :
   Write a class ,
    1. by extending Thread class
    2. implement interface 'Runnable' - Runnable is the best way to create a Thread. Because using Thread class we lose the usage of multiple inheritance.

- There will be a 'Thread Scheduler', which creates Thread in stack and gives to us only when there is memory in stack.
- There is no guarantee that Threads will always run one after other. - This is called 'MultiThreading'. MultiThreading runs Threads concurrently. we can't guess the output.
- Threads usage involves mostly in Banking and Insurance Domains.
- we can sleep a Thread with - Thread.sleep(1000);

1. public class MyThread1 extends Thread {

        // for thread, it only requires a 'run' method which is inside 'Runnable'. so we need to override it.
        // we should mandatorily override the run() method
        // we write the logic inside the run() method

        @Override
        public void run() {
            System.out.println(Thread.currentThread().getName());
            System.out.println("Hi...");
        }

   }

    public class Test {

        // main() itself is also a Thread
        public static void main(String[] args) {

            System.out.println(Thread.currentThread().getName());  // to know the name of the current Thread. as main() is also a thread.
            MyThread1 myThread1 = new MyThread1();

            -- not needed this -- myThread1.run();   // but we need to call the start() method which will call run() method, then only the thread will be created.
            // with run() method, the Thread won't gets registered
            myThread1.start();

            // start() method is from Thread class, Not from Runnable Interface
            // So when we create a Thread by implementing with Runnable Interface,
            //      we need to create an object of Runnable and we need to pass that object while creating the Thread and then we need to call the start() with this Thread object.

        }

    }

- join() method : It stops the current thread until the caller thread is executed.

    Ex:
    main    - is a thread
        (inside main)
        t1.join()
        t2.join()
    -- here, t1 and t2 gets executed and main gets stopped. main will continue until t1 and t2 completed execution.

    Ex: T1 {
            t2.join()
        }
        // here, t2 gets executed and T1 gets stopped. T1 will continue until t2 completes execution.

- When two Threads try to update a same variable at same time , value won't gets updated properly. Sometimes they update the same value. This is known as 'Race Condition'
- To overcome Race condition - we do use 'Synchronized'. We can make a method synchronized. and we can also create a block inside method as synchronized.
- Synchronized :
        1. Only one Thread can access a method at one time
        2. Thread acquires a lock
        3. Slow in operations
        4. Thread safe - only 1 thread can access at one time.
   -- In Banking all debit operations are Thread safe i.e. Synchronized debit operations. It won't allow to debit at same time.
   -- But for Credit operations, it can happen parallelly, so we don't make them Synchronized.

- In Threads, with join() in some scenarios there occurs 'Deadlock'. Ex: t1 waits for t2 and t2 is waiting for t1.

- LifeCycle of a Thread :
  5 Steps -
    1. New
          create a Thread
    2. Runnable
    3. Running
    4. Blocked/waiting -> Runnable
    5. Terminated
